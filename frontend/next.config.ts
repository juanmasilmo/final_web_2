import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // reactStrictMode: activa advertencias adicionales de React en desarrollo
  reactStrictMode: true,
};

export default nextConfig;
